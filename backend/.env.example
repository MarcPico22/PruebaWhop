# ====================
# CONFIGURACIÓN GENERAL
# ====================
PORT=3000
DATABASE_URL=./data.db
BASE_URL=http://localhost:3000

# Secret para JWT (cambiar en producción)
JWT_SECRET=tu_secret_super_seguro_cambiame_en_produccion

# ====================
# SISTEMA DE BILLING Y SUSCRIPCIONES
# ====================
# Stripe Billing para cobrar suscripciones a las empresas (NO confundir con el Stripe del usuario)
# Estos valores son REQUERIDOS para el sistema de billing
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Price IDs de Stripe para cada plan (crea los productos en Stripe Dashboard)
STRIPE_PRICE_MONTHLY_PRO=price_...
STRIPE_PRICE_YEARLY_PRO=price_...
STRIPE_PRICE_MONTHLY_ENTERPRISE=price_...
STRIPE_PRICE_YEARLY_ENTERPRISE=price_...

# ====================
# SEGURIDAD - ENCRIPTACIÓN
# ====================
# Secret para encriptar API keys en la base de datos (AES-256)
# Genera uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_SECRET=cambiame_por_un_secret_aleatorio_de_64_caracteres_hexadecimal

# ====================
# OPCIONAL: KEYS DE DEMO
# ====================
# Estas keys son OPCIONALES y solo se usan si un tenant no ha configurado sus propias keys.
# Útil para testing y desarrollo. En producción, cada tenant debe configurar sus propias keys.
# Para configurar las keys de tu empresa, ve a: Configuración → Integraciones

# Stripe Demo (opcional)
DEMO_STRIPE_SECRET_KEY=sk_test_...
DEMO_STRIPE_PUBLISHABLE_KEY=pk_test_...

# SendGrid Demo (opcional)
DEMO_SENDGRID_API_KEY=SG....
DEMO_FROM_EMAIL=no-reply@demo.local

# ====================
# VALORES POR DEFECTO
# ====================
# Intervalos de reintento por defecto (en segundos)
RETRY_INTERVALS=60,300,900
